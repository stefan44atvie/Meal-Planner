{% extends 'base.html.twig' %}

{% block title %}Nutrition{% endblock %}

{% block body %}
    <h1>Nutrition</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nutrition.id }}</td>
            </tr>
            <tr>
                <th>Engery</th>
                <td>{{ nutrition.engery }}</td>
            </tr>
            <tr>
                <th>Fat</th>
                <td>{{ nutrition.fat }}</td>
            </tr>
            <tr>
                <th>Protein</th>
                <td>{{ nutrition.protein }}</td>
            </tr>
            <tr>
                <th>Carbs</th>
                <td>{{ nutrition.carbs }}</td>
            </tr>
            <tr>
                <th>Sugar</th>
                <td>{{ nutrition.sugar }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_nutrition_index') }}">back to list</a>

    <a href="{{ path('app_nutrition_edit', {'id': nutrition.id}) }}">edit</a>

    {{ include('nutrition/_delete_form.html.twig') }}
{% endblock %}
